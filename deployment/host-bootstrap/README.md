# Smainer Provider Host Bootstrap Package

**Target Host:** 10.100.102.208  
**Package Version:** 1.0.0  

## Quick Start

```bash
# Make scripts executable
chmod +x *.sh

# Run installation sequence
sudo ./install-dependencies.sh
sudo ./configure-env.sh  
sudo ./install-systemd.sh
sudo ./verify-setup.sh

# Start the daemon
sudo provider-daemon start
sudo provider-daemon status
```

## Package Contents

| File | Purpose |
|------|---------|
| `install-dependencies.sh` | Install system dependencies and Python environment |
| `configure-env.sh` | Configure environment variables securely |
| `install-systemd.sh` | Install and configure systemd service |
| `verify-setup.sh` | Comprehensive setup verification |
| `RUNBOOK.md` | Detailed installation and management guide |
| `README.md` | This file |

## Installation Order

1. **Dependencies** → System packages, Python environment, user setup
2. **Environment** → Secure configuration with credentials  
3. **Systemd** → Service installation and auto-start setup
4. **Verification** → End-to-end testing and validation

## Key Features

- ✅ **Security First:** Private keys stored with restricted permissions (600)
- ✅ **Systemd Hardening:** Service runs with security restrictions and isolation
- ✅ **Non-Interactive:** Minimal prompts, suitable for automation
- ✅ **Comprehensive Verification:** Tests all components and connectivity
- ✅ **Easy Management:** Simple `provider-daemon` command for all operations
- ✅ **Detailed Logging:** Full audit trail of installation and operations

## Management Commands

```bash
provider-daemon start      # Start the service
provider-daemon stop       # Stop the service  
provider-daemon restart    # Restart the service
provider-daemon status     # Check status
provider-daemon logs       # Follow live logs
provider-daemon kill       # Emergency force kill
```

## Support

- **Full documentation:** See `RUNBOOK.md`
- **Verification logs:** Generated by `verify-setup.sh`
- **Service logs:** Available via `provider-daemon logs`

---
Generated: May 4, 2026