name: Submodule Sync Notice

"on":
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Explain submodule sync policy
        run: echo "Submodule updates are committed explicitly from the monorepo."